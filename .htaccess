<IfModule mod_rewrite.c>
  RewriteEngine On
  # Avoid redirect loop if the request is already within the /public folder
  RewriteCond %{REQUEST_URI} !^/public/
  # Redirect all requests to the /public folder
  RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
